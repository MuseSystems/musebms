defmodule MscmpSystFormsTest do
  use ExUnit.Case
  doctest MscmpSystForms

  # TODO: Naturally, this we're not testing anything with what's written here.
  #       Currently studying the issue and making decisions on we can best test
  #       components and their combined outputs.

  test "Closes eyes and pretends to test something." do
    assert true
  end
end
